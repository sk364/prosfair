This is directory for Graphical User Interface

We are using pygame which can be installed on ubuntu with:

apt-get install python-pygame

To run the simple gui for testing pygame, simply run:

python gui_basic.py




